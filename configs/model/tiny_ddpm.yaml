# @package _global_

model:
  name: TinyCondDDPM
  architecture:
    hidden_dim: 64
    t_emb_dim: 32

  diffusion:
    timesteps: 100
    beta_start: 0.0001
    beta_end: 0.02
    schedule: linear

  # Normalization (handled automatically)
  normalize_features: true
  normalize_targets: true
